services:
  gcap-statement:
    image: ${REGISTRY}/gcap-statement:${TAG}
    container_name: gcap-statement

    ports:
      - "${SERVER_PORT}:3000"

    environment:
      NEXT_PUBLIC_BASE_URL: ${NEXT_PUBLIC_BASE_URL}
      NEXT_PUBLIC_PRICE_BASE_URL: ${NEXT_PUBLIC_PRICE_BASE_URL}
      